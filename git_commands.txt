There are the commands_________________________________________________
    rm testing.py
   ls
   clear
   git init
  ls -a  =Lists all files including hidden ones linux
   ls -Force... = Lists all files including hidden ones windows
   Get-ChildItem -Force... = Lists all files including hidden ones windows
   git status
   git add testing.py
   git rm --cached testing.py
  
   git add .
   git config user.name "pranjali pushkar"
  git config user.email "pranjalipushkar@gmail.com"
   git commit -m "added testing files"
   rm testing.py
  git restore testing.py

  git remote add origin git@github.com:PranjaliPushkar/git_train_with_shubham.git
  git remote -v
  git remote set-url origin https://ghp_X9stJGYiBt@github.com/PranjaliPushkar/git_train_with_shubham.git    
  git push -u origin master
